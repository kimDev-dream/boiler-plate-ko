import React from 'react';

const RegisterPage = props => {
    return (
        <div>
            RegisterPage
        </div>
    );
};

RegisterPage.propTypes = {
    
};

export default RegisterPage;